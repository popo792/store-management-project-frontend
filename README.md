# Relational Store Management System - Frontend

The user interface for the Store Management System, allowing users to interact with customer, employee, product, and order records managed by the Spring Boot backend.

## Tech Stack
* **Frontend:** HTML, CSS, JavaScript

## Setup
1. Clone the repository.
2. Ensure the Spring Boot backend is running on `localhost:8080`.
3. Open the main HTML file to interact with the API.
